name: test

on:
  push:

jobs:
  jobtest:
    runs-on: ubuntu-latest

    steps:
      - name: recup projet
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'

      - name: dependances
        run: mvn install

      - name: build
        run: mvn package
